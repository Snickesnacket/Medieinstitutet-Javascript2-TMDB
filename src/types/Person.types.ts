import { IMovies } from './Movies.types';

export interface IPerson {
  gender: number;
  id: number;
  name: string;
  profile_path: string;
  cast_id: number;
  character: string;
  credit_id: string;
}
